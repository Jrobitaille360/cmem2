# v2.6.0 — Checklist déploiement production

Date : 2026-05-10

## Migrations SQL

Les deux migrations sont **déjà appliquées en production** (appliquées manuellement avant le déploiement de la Phase 1) :

- [x] `20260505_subscriptions_purchase_token_unique.sql` — contrainte `uq_purchase_token_app` + migration données Google Play
- [x] `20260508_stripe_idempotency.sql` — table `stripe_processed_events`

## Variables d'environnement

Aucune nouvelle variable obligatoire.

Variable optionnelle déjà documentée dans `.env.example` :

```
PUZZLE_GOOGLE_SERVICE_ACCOUNT_JSON=/chemin/vers/service-account.json
```

## Déploiement du code

- [ ] `git pull origin main` sur le serveur
- [ ] `composer install --no-dev --optimize-autoloader`
- [ ] Vérifier que `/health` répond `200`

## Vérifications post-déploiement

- [ ] `GET /puzzle/auth/subscription-status` — réponse correcte avec device_token Google Play
- [ ] `POST /puzzle/auth/link-device` — disponible (retourne 401 sans JWT, 200 avec JWT valide)
- [ ] Stripe webhook : envoyer un événement test → vérifier `stripe_processed_events` ne crée pas de doublon
- [ ] Cron `backup_uploads.php` — vérifier qu'une archive `.tar.gz` est créée correctement

## Crons

Aucun nouveau cron. Cron existant `backup_uploads.php` modifié (PharData → tar) — aucun changement de configuration requis.

## Rollback

En cas de problème :

1. `git checkout v2.5.0`
2. `composer install --no-dev --optimize-autoloader`
3. La contrainte `uq_purchase_token_app` et la table `stripe_processed_events` sont rétrocompatibles — pas besoin de reverter la DB.
