# v2.17.3 — Checklist déploiement production

## Migrations SQL

- [x] `docs/v-2-17-3/20260828_push_show_entity_detail.sql` — `ALTER TABLE notification_prefs
      ADD COLUMN show_entity_detail TINYINT(1) NOT NULL DEFAULT 0`. Déjà appliquée sur
      dev-cmem2 **et prod** (2026-08-27, hors déploiement de code — colonne nullable-safe,
      défaut 0, aucun impact tant que le code n'est pas déployé).

## `.env`

- [ ] `APP_VERSION=2.17.3` (`private/utilitaires/.env.prod`, injecté par `deploy.ps1`).
- [ ] Aucune nouvelle variable d'environnement requise pour cette version.

## Cron

- [ ] Aucun changement de configuration crontab. `send_push_notifications.php` lit
      `show_entity_detail` depuis `notification_prefs` (colonne déjà en place) — aucune
      action requise.

## Vérifications post-déploiement

- [ ] `GET /push/preferences` (compte de test) → chaque kind retourne
      `show_entity_detail: false` par défaut.
- [ ] `PUT /push/preferences` avec `show_entity_detail: true` sur un kind → persiste et se
      relit correctement.
- [ ] `php private/tests/test_push.php` contre prod (compte de test) ou au minimum contre
      dev-cmem2 avant bascule — déjà validé sur dev-cmem2 (114/114).

## Composer

- [ ] `composer install --no-dev --optimize-autoloader` (fait par `deploy.ps1` côté serveur).

## Connu / hors scope

- 8 échecs pré-existants dans `run_all_tests.php`, non liés à cette release : flake
  seconde près sur `If-Unmodified-Since` (`test_calendars.php`, `test_ics_todos_e2e.php`,
  `test_projets.php` — voir `docs/PLAN_concurrence-updated-at-microsecondes.md`, risque
  accepté 2026-08-14) et `test_stripe_webhooks.php` (AC5 marqué `EXPECTED FAIL` dans le
  fichier lui-même ; AC6/AC7 et `body.received` désynchronisés de l'implémentation réelle —
  à investiguer séparément).
