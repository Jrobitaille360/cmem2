# v2.17.4 — Checklist déploiement production

## Migrations SQL

- [ ] Aucune. Aucune modification de schéma dans cette version.

## `.env`

- [ ] `APP_VERSION=2.17.4` (`private/utilitaires/.env.prod`, injecté par `deploy.ps1`).
- [ ] Aucune nouvelle variable d'environnement requise pour cette version.

## Cron

- [ ] Aucun changement de configuration crontab. `send_push_notifications.php` utilise le
      même `DueScanner` — seul le calcul du `body` change, pas la sélection des échéances
      ni la fréquence.

## Vérifications post-déploiement

- [ ] Créer un événement ponctuel de test (échéance proche) → le push reçu affiche
      `HHhMM - HHhMM` dans son `body` (pas « dans X minutes »).
- [ ] Créer une tâche à échéance de test → le `body` affiche la date/heure d'échéance.
- [ ] `php private/tests/test_push.php` contre prod (compte de test) ou au minimum contre
      dev-cmem2 avant bascule — déjà validé sur dev-cmem2 (126/126, section 8c).

## Composer

- [ ] `composer install --no-dev --optimize-autoloader` (fait par `deploy.ps1` côté serveur).
      Aucune nouvelle dépendance dans cette version.

## Connu / hors scope

- 6 échecs pré-existants dans `run_all_tests.php`, non liés à cette release : flake
  seconde près sur `If-Unmodified-Since` (voir `docs/PLAN_concurrence-updated-at-microsecondes.md`)
  et `test_stripe_webhooks.php` (AC5/AC6/AC7 désynchronisés de l'implémentation réelle —
  déjà connu, non aggravé par cette release).
