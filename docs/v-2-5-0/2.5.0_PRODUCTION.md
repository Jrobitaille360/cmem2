# v2.5.0 — Checklist déploiement production

## Migrations SQL

- [ ] Appliquer `docs/v-2-4-1/20260430_files_accessibility.sql`

```bash
mysql -u root -p cmem2_db < docs/v-2-4-1/20260430_files_accessibility.sql
```

Étend le type ENUM de la colonne `accessibility` dans la table `files` :
`ENUM('public','private')` → `ENUM('public','private','grand-public')`.

Aucune migration SQL pour le module d'abonnement (table `subscriptions` inchangée).

## Variables `.env`

Aucune nouvelle variable requise.

- [ ] Vérifier que `APP_VERSION=2.5.0` est à jour dans `.env`

## Dépendances

- [ ] `composer install --no-dev --optimize-autoloader`

## Vérifications post-déploiement

- [ ] `GET /health` → `200 OK`
- [ ] `POST /subscription/portal` avec JWT valide et `stripe_customer` en base → `200` avec `portal_url`
- [ ] `POST /subscription/portal` sans `stripe_customer` en base → `404` avec `errors.error = "NO_SUBSCRIPTION"`
- [ ] `POST /subscription/portal` sans JWT → `401`
- [ ] `POST /files` avec `accessibility=grand-public` (admin) → `201`
- [ ] `GET /files/{id}` sur un fichier `grand-public` sans JWT → `200`
- [ ] `GET /files/{id}/info` sur un fichier `grand-public` sans JWT → `200`

## Cron / services

Aucun nouveau cron requis.
