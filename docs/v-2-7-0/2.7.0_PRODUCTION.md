# Checklist déploiement — v2.7.0

## Pré-déploiement

- [ ] Tests locaux : `php private/tests/run_all_tests.php` → 0 échec
- [ ] Backup complet de la production avant migration SQL

## Migration SQL

**Ordre d'application (sur prod) :**

```bash
mysql -u root -p < docs/v-2-7-0/20260523_v270_migration.sql
mysql -u root -p < docs/v-2-7-0/20260524_playstore_subscriptions_device_uuid.sql
mysql -u root -p < docs/v-2-7-0/20260529_backup_json_devices.sql
```

Ou depuis zéro :

```bash
mysql -u root -p < docs/v-2-7-0/build_DB-v-2.7.0.sql
```

**Tables supprimées :**

- `puzzle_devices`
- `subscriptions`

**Tables créées :**

- `android_devices`
- `web_devices`
- `app_user_settings`
- `playstore_subscriptions`
- `stripe_subscriptions`

**Colonnes ajoutées :**

- `android_devices.backup_json`, `android_devices.backup_saved_at`
- `web_devices.backup_json`, `web_devices.backup_saved_at`

**Tables modifiées :**

- `puzzle_shared` — FK creator_id/partner_id → users (était puzzle_devices)
- `puzzle_shared_pieces` — nouveau modèle d'état (state, held_by_id, by_id)
- `puzzle_shared_events` — nouveau modèle d'état (state, held_by_id, by_id)

## Déploiement code

```bash
git pull origin main
composer install --no-dev --optimize-autoloader
composer dump-autoload
```

## Crons à mettre à jour

Remplacer dans crontab :

```cron
# ANCIEN — supprimer :
0 3 * * * php /path/to/src/cron/expire_subscriptions.php

# NOUVEAUX — ajouter :
10 3 * * * php /path/to/src/cron/expire_playstore.php >> /path/to/logs/cron.log 2>&1
20 3 * * * php /path/to/src/cron/expire_stripe.php >> /path/to/logs/cron.log 2>&1
```

## Stripe Dashboard (prod)

- [x] Webhook `cmem2 API` live → `https://cmem2.journauxdebord.com/v2/billing/webhook`
- [ ] Vérifier dans `logs/app-YYYY-MM-DD.log` que les événements Stripe arrivent sur `/v2/billing/webhook`

## Vérifications post-déploiement

- [ ] `GET /health` → 200
- [ ] `POST /v2/devices/android/register` sans JWT → 200
- [ ] `POST /v2/devices/web/register` sans JWT → 200
- [ ] `GET /v2/puzzle/carousel` avec device_token → 200
- [ ] `POST /v2/billing/webhook` reçoit événements Stripe (vérifier logs)
- [ ] Routes legacy `/puzzle/auth/register-device` → 410 ✓
- [ ] Routes legacy `/stripe/webhook` → 404 (route supprimée) ✓
