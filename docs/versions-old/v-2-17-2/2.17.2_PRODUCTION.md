# v2.17.2 — Checklist déploiement production

## Migrations SQL

- [x] Aucune.

## `.env`

- [x] `APP_VERSION=2.17.2` (`private/utilitaires/.env.prod`, injecté par `deploy.ps1`).
- [x] `CMEMWEB_APP_URL` déjà présente en prod depuis v2.16.0 (module booking) — réutilisée
      telle quelle, aucune nouvelle variable.

## Cron

- [ ] Aucun changement.

## Vérifications post-déploiement

- [ ] `POST /groups/{id}/invite` (compte de test) → `data.invite_url` commence par
      `CMEMWEB_APP_URL`, pas le domaine `cmem2.journauxdebord.com`.
- [ ] `php private/tests/test_groups.php` contre prod (compte de test) — déjà validé sur
      dev-cmem2 (68/68).
- [ ] `deploy.ps1` corrigé pour scanner `docs/docs-api/*` (au lieu de `docs/*`) pour le
      transfert des `API_*ENDPOINTS.json` publics — vérifier `GET /entrypoints` liste bien
      tous les modules après déploiement prod (régression potentielle introduite par le
      reclassement `docs/` de la release précédente, corrigée dans ce cycle).

## Composer

- [ ] `composer install --no-dev --optimize-autoloader` (fait par `deploy.ps1` côté serveur).

## Connu / hors scope

- 8 échecs pré-existants dans `run_all_tests.php`, non liés à cette release :
  flake seconde près sur `If-Unmodified-Since` (`test_calendars.php`, `test_projets.php` —
  voir `docs/PLAN_concurrence-updated-at-microsecondes.md`, risque accepté 2026-08-14) et
  `test_stripe_webhooks.php` (AC5 marqué `EXPECTED FAIL` dans le fichier lui-même ; AC6/AC7 et
  `body.received` désynchronisés de l'implémentation réelle — à investiguer séparément).
