# v2.17.0 — Checklist déploiement production

## Migrations SQL

- [ ] Appliquer `docs/v-2-17-0/20260814_time_sessions.sql` (ou `build_DB-v-2.17.0.sql` complet sur
      une base neuve) — crée la table `time_sessions`. Aucun impact sur les tables existantes.
  - Vérifier avant application : `calendar_todos.id` est `int(10) unsigned` en prod (comme en dev) —
    sinon la contrainte `fk_time_sessions_todo` échoue en errno 150. `SHOW CREATE TABLE
    calendar_todos\G` pour confirmer.

## `.env`

- [ ] `APP_VERSION=2.17.0` (déjà dans `private/utilitaires/.env.prod`, injecté par `deploy.ps1`).
- [ ] Aucune nouvelle variable d'environnement requise pour cette version.

## Cron

- [ ] Aucun changement.

## Vérifications post-déploiement

- [ ] `GET /entrypoints/ics` renvoie la section `time_sessions`.
- [ ] `POST /calendars/{id}/todos/{todoId}/time-sessions/start` répond `201` sur un compte de test.
- [ ] `php private/tests/test_time_sessions.php` contre la cible prod (si un compte de test existe)
      ou au minimum contre dev-cmem2 avant bascule.

## Composer

- [ ] `composer install --no-dev --optimize-autoloader` (fait par `deploy.ps1` côté serveur).
