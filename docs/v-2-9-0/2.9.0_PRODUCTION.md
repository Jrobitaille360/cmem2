# cmem2 API — v2.9.0 — Checklist déploiement production

## 1. Migrations SQL (`docs/v-2-9-0/`)

Statut connu au moment de la release (à revérifier en prod avant d'exécuter quoi que ce soit) :

| Fichier | Statut | Note |
| - | - | - |
| `20260626_fix_dragon_biome.sql` | Déjà appliqué (dev+prod, v2.8.0) | Data fix seed `monsters`, no-op si déjà correct |
| `20260630_show_question_to_player.sql` | Appliqué dev | **Vérifier prod avant déploiement** |
| `20260715_calendar_tags.sql` | Appliqué dev | **Vérifier prod avant déploiement** |
| `20260715_calendar_journals_todos_fk.sql` | Appliqué dev+prod | Nettoyage orphelins inclus, no-op si 0 lignes |
| `20260715_users_cmem_plan_override.sql` | Appliqué dev+prod | Colonne déjà utilisée en prod (comptes admin) |
| `20260716_add_superadmin_role.sql` | **NON appliqué** | **STOP — confirmation explicite requise** (ALTER sur `users`, table partagée toutes apps). Après exécution, assigner manuellement au moins un compte `SUPERADMINISTRATEUR` via `UPDATE users SET role = 'SUPERADMINISTRATEUR' WHERE id = ...` — aucun endpoint API ne permet cette attribution |
| `20260718_calendar_shares_group_id.sql` | Appliqué dev+prod | — |

Le fichier cumulatif `docs/v-2-9-0/build_DB-v-2.9.0.sql` reflète le schéma final avec ces
migrations intégrées — sert de référence pour une nouvelle installation, pas pour une mise à
jour incrémentale (utiliser les fichiers `YYYYMMDD_*.sql` individuels sur une base existante).

## 2. `.env` / `.env.example`

- `APP_VERSION=2.9.0`

## 3. Installation / mise à jour

```bash
composer install --no-dev --optimize-autoloader
```

## 4. Crons

Aucun nouveau cron introduit dans cette version.

## 5. Vérifications post-déploiement

- [ ] `GET /entrypoints` répond 200 sans authentification
- [ ] `POST /calendars/{id}/share` avec `group_id` fonctionne (test manuel ou `test_calendars.php` section 13bis)
- [ ] `GET/POST /calendars/{id}/tags` fonctionne
- [ ] Checkout Stripe : vérifier que `stripe_subscriptions` est bien créée après paiement (`metadata` racine de session)
- [ ] Suite de tests complète : `php private/tests/run_all_tests.php` → 0 échec
