# v2.17.1 — Checklist déploiement production

## Migrations SQL

- [x] Aucune. `contacts.supprime_le`, `projects.deleted_at` et `calendar_todos.deleted_at`
      existaient déjà en base ; cette release ajoute seulement du code (routes/requêtes) qui
      les exploite désormais pleinement — `projects.deleted_at` était présent mais jamais écrit
      avant cette version.

## `.env`

- [ ] `APP_VERSION=2.17.1` (déjà dans `private/utilitaires/.env.prod`, injecté par `deploy.ps1`).
- [ ] Aucune nouvelle variable d'environnement requise pour cette version.

## Cron

- [ ] Aucun changement.

## Vérifications post-déploiement

- [ ] `GET /entrypoints` liste `contacts/API_CONTACTS_ENDPOINTS.json` et
      `projets/API_PROJETS_ENDPOINTS.json`.
- [ ] `GET /contacts/deleted` répond `200` sur un compte de test avec au moins une fiche
      soft-supprimée.
- [ ] `DELETE /projets/projects/{id}` puis `GET /projets/projects/{id}` → `404`, et la ligne
      reste en base (`SELECT deleted_at FROM projects WHERE id=?` non NULL) — pas de perte des
      tâches/calendrier caché associés.
- [ ] `php private/tests/test_contacts.php` et `php private/tests/test_projets.php` contre prod
      (compte de test) ou au minimum contre dev-cmem2 avant bascule — déjà validé sur dev-cmem2
      lors du développement (148/148 et 106/106).

## Composer

- [ ] `composer install --no-dev --optimize-autoloader` (fait par `deploy.ps1` côté serveur).
