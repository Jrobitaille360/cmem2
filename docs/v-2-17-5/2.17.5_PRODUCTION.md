# v2.17.5 — Checklist déploiement production

## Migrations SQL

- [ ] `docs/v-2-17-5/20260901_stripe_subscriptions_is_premium.sql` — `ALTER TABLE
      stripe_subscriptions ADD COLUMN is_premium` (colonne générée `STORED`, dérivée de
      `status`). Déjà appliquée sur dev-cmem2, à appliquer sur prod avant/avec ce
      déploiement (STOP — confirmation explicite requise avant `ALTER TABLE` en prod).
  - Commande :

    ```bash
    ssh journaux_de_bord \
      "mysql -h journauxdebord.com -u <DB_USER> -p'<DB_PASS>' <DB_NAME_PROD> < /dev/stdin" \
      < docs/v-2-17-5/20260901_stripe_subscriptions_is_premium.sql
    ```

## `.env`

- [ ] `APP_VERSION=2.17.5` (`private/utilitaires/.env.prod`, injecté par `deploy.ps1`).
- [ ] Aucune nouvelle variable d'environnement requise pour cette version.

## Vérifications post-déploiement

- [ ] `DESCRIBE stripe_subscriptions` sur prod → colonne `is_premium` présente
      (`tinyint(1) unsigned`, `STORED GENERATED`).
- [ ] `php private/tests/test_stripe_webhooks.php` contre prod (ou au minimum re-confirmé sur
      dev-cmem2 avant bascule — déjà validé, 14/14 sections A/B/C).
- [ ] Déclencher un événement Stripe réel (ou test mode) `customer.subscription.updated` →
      vérifier `is_premium=1` sur la ligne concernée.

## Composer

- [ ] `composer install --no-dev --optimize-autoloader` (fait par `deploy.ps1` côté serveur).
      Aucune nouvelle dépendance dans cette version.

## Connu / hors scope

- 3 échecs pré-existants dans `run_all_tests.php`, non liés à cette release : flake seconde
  près sur `If-Unmodified-Since` (`test_calendars.php` 7.6/16e.6e, `test_projets.php` 3.7v4 —
  voir `docs/PLAN_concurrence-updated-at-microsecondes.md`, décision Option C, aucune
  migration prévue).
